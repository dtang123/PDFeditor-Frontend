import { styled } from "styled-components";
import { NameContainer, OwnerContainer } from "../drive-listings/drive-listings.styles";

export const SmallNameContainer = styled(NameContainer) `
    width: 50%
`
export const LargeOwnerContainer = styled(OwnerContainer)`
    width: 25%
`